apiVersion: v1
kind: PersistentVolume
metadata:
  name: mongo-auth-pv-development
spec:
  capacity:
    storage: 3Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: /k8s-mongo-auth-pv-development
