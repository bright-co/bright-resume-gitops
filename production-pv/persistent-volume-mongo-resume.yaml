apiVersion: v1
kind: PersistentVolume
metadata:
  name: mongo-resume-pv-production
spec:
  capacity:
    storage: 3Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: /k8s-mongo-resume-pv-production
